- name: Instalacion de MySQL con RPM
  hosts: all
  tasks:
    - name: Ejecucion de Archivo RPM
      command: rpm -ivh mysql57-community-release-el7-9.noarch.rpm
    - name: Instalar MySQL Server
      yum:
        name: mysql-server
        state: latest
    - name: Start MySQL
      command: systemctl start mysqld
    - name: Obtener Password
      command: grep 'temporary password' /var/log/mysqld.log
      register: password-mysql
    - name:
      command: printf '{{password-mysql.stdout}}\nTsoft*2019\ny\ny\ny\ny\ny\ny\n' | mysql_secure_installation