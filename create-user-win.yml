---
- name: Creación de usuario Windows
  hosts: all
  gather_facts: yes
  tasks:
    - name: Ensure user bob is present
      win_user:
        name: "{{ user }}"
        password: "{{ pass }}"
        state: present
        groups:
          - Administrators
